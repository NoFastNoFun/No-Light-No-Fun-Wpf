<UserControl x:Class="No_Fast_No_Fun_Wpf.Views.Controls.MatrixMonitoringControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2D2D30" Padding="10">
            <StackPanel>
                <TextBlock Text="Matrix Monitoring Preview" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,5"/>
                <TextBlock Text="Shows what the actual LED matrix should display based on DMX routing" FontSize="12" Foreground="LightGray"/>
            </StackPanel>
        </Border>

        <!-- Matrix Display -->
        <Border Grid.Row="1" Background="Black" BorderBrush="#404040" BorderThickness="1" Margin="10">
            <Grid>
                <Image Source="{Binding Bitmap}" Stretch="Uniform" RenderOptions.BitmapScalingMode="NearestNeighbor"/>

                <!-- Overlay for stats -->
                <Border VerticalAlignment="Top" HorizontalAlignment="Right" Background="#80000000" Margin="10" Padding="10" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="{Binding ActivePixels, StringFormat='Active Pixels: {0}'}" Foreground="White" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TotalPixels, StringFormat='Total Pixels: {0}'}" Foreground="LightGray"/>
                        <TextBlock Text="{Binding FramesPerSecond, StringFormat='FPS: {0}'}" Foreground="LightGray"/>
                    </StackPanel>
                </Border>

                <!-- No data overlay -->
                <Border Background="#80000000" Visibility="{Binding Bitmap, Converter={StaticResource NullToVisibilityConverter}}">
                    <TextBlock Text="No matrix data available" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16"/>
                </Border>
            </Grid>
        </Border>

        <!-- Controls -->
        <Border Grid.Row="2" Background="#2D2D30" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button Content="Refresh Display" Command="{Binding RefreshCommand}" Padding="10,5" Margin="0,0,10,0"/>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Matrix Size: " Foreground="White" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox Width="80" SelectedIndex="0">
                        <ComboBoxItem Content="64x64"/>
                        <ComboBoxItem Content="128x128"/>
                        <ComboBoxItem Content="256x256"/>
                    </ComboBox>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl> 